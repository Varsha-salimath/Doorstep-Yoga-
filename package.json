{
  "name": "doorstep-yoga",
  "private": true,
  "scripts": {
    "build": "npm --prefix backend ci && npm --prefix backend run build",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}
